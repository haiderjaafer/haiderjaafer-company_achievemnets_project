'use client';

import React, { useState, useEffect } from "react";
import { motion } from "framer-motion";

import { ArrowRight, Sparkles } from "lucide-react";
import { ThreeBackground } from "../components/ThreeBackground";
import { AnimatedCarousel } from "../components/AnimatedCarousel";
import { ParallaxSection } from "../components/ParallaxSection";

const carouselItems = [
  {
    id: 1,
    title: "Stunning Mountain Vista",
    description: "Experience breathtaking views of majestic mountains",
    imageUrl: "https://images.unsplash.com/photo-1506905925346-21bda4d32df4?w=1200&h=600&fit=crop",
    category: "Nature",
  },
  {
    id: 2,
    title: "Urban Architecture",
    description: "Modern design meets classic elegance",
    imageUrl: "https://images.unsplash.com/photo-1480714378408-67cf0d13bc1b?w=1200&h=600&fit=crop",
    category: "Architecture",
  },
  {
    id: 3,
    title: "Ocean Serenity",
    description: "Tranquil waters and golden sunsets",
    imageUrl: "https://images.unsplash.com/photo-1505142468610-359e7d316be0?w=1200&h=600&fit=crop",
    category: "Landscape",
  },
  {
    id: 4,
    title: "Forest Dreams",
    description: "Lush greenery and peaceful trails",
    imageUrl: "https://images.unsplash.com/photo-1441974231531-c6227db76b6e?w=1200&h=600&fit=crop",
    category: "Nature",
  },
];

const containerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: {
      staggerChildren: 0.2,
      delayChildren: 0.3,
    },
  },
};

const itemVariants = {
  hidden: { opacity: 0, y: 20 },
  visible: {
    opacity: 1,
    y: 0,
    transition: {
      duration: 0.8,
    },
  },
};

export default function Home() {
  const [isScrolled, setIsScrolled] = useState(false);

  useEffect(() => {
    const handleScroll = () => {
      setIsScrolled(window.scrollY > 50);
    };

    window.addEventListener("scroll", handleScroll);
    return () => window.removeEventListener("scroll", handleScroll);
  }, []);

  return (
    <div className="relative w-full overflow-hidden bg-slate-950">
      {/* <ThreeBackground /> */}
{/* 
      <motion.header
        initial={{ opacity: 0, y: -20 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.8 }}
        className={`fixed top-0 left-0 right-0 z-40 transition-all duration-300 ${
          isScrolled ? "bg-slate-900/80 backdrop-blur-md border-b border-slate-700" : "bg-transparent"
        }`}
      >
        <div className="container mx-auto px-4 py-4 flex items-center justify-between">
          <motion.div
            whileHover={{ scale: 1.05 }}
            className="flex items-center gap-2 text-white font-bold text-2xl"
          >
            <Sparkles size={28} className="text-blue-400" />
            <span>AnimateHub</span>
          </motion.div>

          <nav className="hidden md:flex items-center gap-8">
            {["Features", "Carousel", "Gallery", "Contact"].map((item) => (
              <motion.a
                key={item}
                href="#"
                whileHover={{ color: "#3b82f6" }}
                className="text-gray-300 hover:text-blue-400 transition-colors"
              >
                {item}
              </motion.a>
            ))}
          </nav>

          <motion.button
            whileHover={{ scale: 1.05 }}
            whileTap={{ scale: 0.95 }}
            className="px-6 py-2 bg-blue-500 hover:bg-blue-600 text-white rounded-lg font-semibold"
          >
            Get Started
          </motion.button>
        </div>
      </motion.header> */}
{/* 
      <section className="relative min-h-screen flex items-center justify-center pt-20">
        <motion.div
          variants={containerVariants}
          initial="hidden"
          animate="visible"
          className="container mx-auto px-4 text-center text-white z-10"
        >
          <motion.h1
            variants={itemVariants}
            className="text-6xl md:text-7xl font-bold mb-6 bg-linear-to-b from-blue-400 via-purple-400 to-pink-400 bg-clip-text text-transparent"
          >
            Stunning Animations & Carousel
          </motion.h1>

          <motion.p
            variants={itemVariants}
            className="text-xl md:text-2xl text-gray-300 mb-8 max-w-2xl mx-auto"
          >
            Experience the perfect blend of Three.js effects, parallax scrolling, and Framer Motion
            animations
          </motion.p>

          <motion.div
            variants={itemVariants}
            className="flex flex-col md:flex-row gap-4 justify-center mb-12"
          >
            <motion.button
              whileHover={{ scale: 1.05 }}
              whileTap={{ scale: 0.95 }}
              className="px-8 py-3 bg-blue-500 hover:bg-blue-600 text-white rounded-lg font-semibold flex items-center justify-center gap-2"
            >
              Explore Now
              <ArrowRight size={20} />
            </motion.button>

            <motion.button
              whileHover={{ scale: 1.05 }}
              whileTap={{ scale: 0.95 }}
              className="px-8 py-3 text-white border border-white hover:bg-white/10 rounded-lg font-semibold"
            >
              Learn More
            </motion.button>
          </motion.div>

          <motion.div
            animate={{ y: [0, 10, 0] }}
            transition={{ duration: 2, repeat: Infinity }}
            className="text-gray-400 text-sm"
          >
            Scroll to explore
          </motion.div>
        </motion.div>
      </section> */}

      <ParallaxSection className="relative py-20 bg-linear-to-b from-transparent via-slate-900/50 to-transparent">
        <div className="container mx-auto px-4">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
            viewport={{ once: false, amount: 0.3 }}
            className="mb-12"
          >
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-4">Featured Collection</h2>
            <p className="text-gray-400 text-lg">
              Explore our curated collection with auto-scrolling carousel
            </p>
          </motion.div>

          <motion.div
            initial={{ opacity: 0, scale: 0.95 }}
            whileInView={{ opacity: 1, scale: 1 }}
            transition={{ duration: 0.8 }}
            viewport={{ once: false, amount: 0.3 }}
          >
            <AnimatedCarousel items={carouselItems} autoScrollInterval={5000} />
          </motion.div>
        </div>
      </ParallaxSection>

      <ParallaxSection className="relative py-20">
        <div className="container mx-auto px-4">
          <motion.h2
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
            viewport={{ once: false, amount: 0.3 }}
            className="text-4xl md:text-5xl font-bold text-white mb-12 text-center"
          >
            Why Choose Us
          </motion.h2>

          <div className="grid md:grid-cols-3 gap-8">
            {[
              {
                title: "Advanced Animations",
                description: "Powered by Framer Motion and Three.js for stunning visual effects",
                icon: "✨",
              },
              {
                title: "Auto-Scrolling Carousel",
                description: "Smart carousel with 300px height and click-to-detail navigation",
                icon: "🎠",
              },
              {
                title: "Parallax Effects",
                description: "Smooth parallax scrolling for immersive user experience",
                icon: "🌊",
              },
            ].map((feature, index) => (
              <motion.div
                key={index}
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.8, delay: index * 0.2 }}
                viewport={{ once: false, amount: 0.3 }}
                whileHover={{ y: -10 }}
                className="bg-slate-800/50 border border-slate-700 rounded-lg p-8 backdrop-blur-sm"
              >
                <div className="text-4xl mb-4">{feature.icon}</div>
                <h3 className="text-xl font-bold text-white mb-3">{feature.title}</h3>
                <p className="text-gray-400">{feature.description}</p>
              </motion.div>
            ))}
          </div>
        </div>
      </ParallaxSection>

      <ParallaxSection className="relative py-20 bg-linear-to-b from-blue-600/20 via-purple-600/20 to-pink-600/20">
        <div className="container mx-auto px-4 text-center">
          <motion.h2
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8 }}
            viewport={{ once: false, amount: 0.3 }}
            className="text-4xl md:text-5xl font-bold text-white mb-6"
          >
            Ready to Create Something Amazing?
          </motion.h2>

          <motion.p
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8, delay: 0.2 }}
            viewport={{ once: false, amount: 0.3 }}
            className="text-xl text-gray-300 mb-8 max-w-2xl mx-auto"
          >
            Start building your own stunning animated landing page with our powerful components
          </motion.p>

          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.8, delay: 0.4 }}
            viewport={{ once: false, amount: 0.3 }}
            whileHover={{ scale: 1.05 }}
            whileTap={{ scale: 0.95 }}
          >
            <button className="px-8 py-3 bg-linear-to-b from-blue-500 to-purple-500 hover:from-blue-600 hover:to-purple-600 text-white rounded-lg font-semibold flex items-center justify-center gap-2 mx-auto">
              Start Building
              <ArrowRight size={20} />
            </button>
          </motion.div>
        </div>
      </ParallaxSection>

      <footer className="relative border-t border-slate-700 bg-slate-950/80 backdrop-blur-md">
        <div className="container mx-auto px-4 py-12">
          <div className="grid md:grid-cols-4 gap-8 mb-8">
            {[
              { title: "Product", items: ["Features", "Pricing", "Security"] },
              { title: "Company", items: ["About", "Blog", "Careers"] },
              { title: "Resources", items: ["Docs", "API", "Community"] },
              { title: "Legal", items: ["Privacy", "Terms", "Contact"] },
            ].map((section, index) => (
              <motion.div
                key={index}
                initial={{ opacity: 0, y: 20 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.8, delay: index * 0.1 }}
                viewport={{ once: false, amount: 0.3 }}
              >
                <h4 className="text-white font-bold mb-4">{section.title}</h4>
                <ul className="space-y-2">
                  {section.items.map((item) => (
                    <li key={item}>
                      <a href="#" className="text-gray-400 hover:text-blue-400 transition-colors">
                        {item}
                      </a>
                    </li>
                  ))}
                </ul>
              </motion.div>
            ))}
          </div>

          <div className="border-t border-slate-700 pt-8 text-center text-gray-400">
            <p>&copy; 2024 AnimateHub. All rights reserved. Built with Framer Motion & Three.js</p>
          </div>
        </div>
      </footer>
    </div>
  );
}
